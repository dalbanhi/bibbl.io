const MultiSelectInstructions = () => {
    return (
            <p>
                <small>If you don't see any books, add some to your library first!
                <br/>
                To select multiple books, hold the ctrl button (Windows) or the cmd button (macOS).</small>
            </p>
            
    )
}
